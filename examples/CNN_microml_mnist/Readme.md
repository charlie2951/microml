# Demonstration of CNN Inferencing 
## Example: MNIST handwritten digit prediction
A sample jupyter nootbook (.ipynb) is provided to train the model locally (or in Google Colab) and save the trained model as .bin file. Then upload the model into your
board and run inference to predict digit. Sample csv file (digit0 to digit9 data) is provided for prediction.
